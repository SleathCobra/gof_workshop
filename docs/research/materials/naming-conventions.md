# Material naming conventions

Observed families include identical stems, `*_diffuse`, detail/variant suffixes and model/texture files placed in analogous mesh/texture folder trees. Exact stem and normalized suffix matches receive higher confidence than directory proximity.

Low-confidence candidates are never silently promoted. The AEM Inspector displays confidence and effect, manual choice persists in the workspace, and clearing it returns to automatic resolution.
