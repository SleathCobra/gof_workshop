# Cross-platform material findings

GOF2 PC, Android, iOS and macOS share many AEM/AEI basenames even when the texture codec differs. This supports relationship transfer by logical asset identity, while decoded surface orientation and codec remain profile-specific.

GOF3D names and layouts are not merged into the GOF2 resolver. Any older-engine corroboration is labeled research evidence only.
