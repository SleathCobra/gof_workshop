# External material-reference evidence

Engine research shows resource lookup outside the mesh container, and corpus pairs exhibit strong model/texture stem and folder conventions. Visual checks of high-confidence `*_diffuse.aei` matches produce coherent UV mapping on representative models.

This confirms a useful viewer/export relationship, not the authoritative game-effective storage mechanism. Database-driven, model-type and hard-coded mappings remain candidates until a specific external record and engine lookup are correlated.
