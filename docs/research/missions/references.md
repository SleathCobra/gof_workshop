# Mission references

Confirmed runtime references include agent, target station, target system, item/production good, language text, campaign index and current level entities. Dialogue briefing/success content is selected by campaign mission index and language-text ordinal.

IDs are integer indices into runtime arrays or global status. Valid ranges and relocation behavior remain target-version dependent. The dependency graph records these as research relationships, not writable foreign keys.
