# Mission triggers

Observed trigger inputs include campaign index, elapsed time, script step/state, current system/station, mission status value, player/target state and completion of prior dialogue or objectives. These observations come from `LevelScript`, level creation and menu flow.

No serialized trigger opcode table has been found. Numeric switch cases in executable logic must not be relabeled as an editable disk schema.
